// Demo state is still orchestrated in App because it coordinates leads, activities, modals, and generated files.
// This hook is intentionally small for now and is ready for the V3 deploy automation work.
export default function useDemos() {
  return {}
}
